/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js 15 uses React 19 by default, so if we want to stay with React 18, specify it here
  experimental: {
    // Any Next.js 15 experimental features can be enabled here
  }
};

export default nextConfig;
